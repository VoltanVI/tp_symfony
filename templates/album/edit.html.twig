{% extends 'base.html.twig' %}

{% block title %}Edit Album{% endblock %}

{% block body %}
    <br>
    <h1 class="text-2xl font-semibold text-center text-gray-800 mb-6">Edit Album</h1>

    {{ include('album/_form.html.twig', {'button_label': 'Update'}) }}
    <br>
    <a class="inline-block bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition duration-300" href="{{ path('app_album_index') }}">back to list</a>

    {{ include('album/_delete_form.html.twig') }}
{% endblock %}
