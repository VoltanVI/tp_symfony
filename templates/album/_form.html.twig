{{ form_start(form) }}
    {{ form_widget(form) }}
<br>
    <button class="btn w-full bg-green-600 text-white py-2 px-4 rounded-lg hover:bg-green-700 transition duration-300'">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
